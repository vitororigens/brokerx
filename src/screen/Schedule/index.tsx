import { Text, View } from 'react-native';

export function Schedule() {
  return (
    <View style={{
      flex: 1,
      justifyContent: 'center',
      alignItems:'baseline'
    }}>
      <Text style={{
        textAlign: 'center'
      }}>Agenda</Text>
    </View>
  );
}

